{% extends 'base-back.html.twig' %}


{% block title %}Listes des Reclamations{% endblock %}

{% block body %}
    <div class="content-body">
        <div class="container-fluid">
            <div class="page-titles">
                <h4>La liste des reclamations</h4>

            </div>
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <div class="table-responsive">
                            <p style="100px" > <input  id="myInput"  type="text" placeholder="search">
                            <table id="t" class="table table-hover table-responsive-sm">
                                <thead>
                                <tr>


                                    {#  <th class="text-center">Supprimer</th>
                                    <th class="text-center">Modifier</th>#}
                                    <th class="text-center"> date reclamation</th>
                                    <th class="text-center">type reclamation</th>
                                    <th class="text-center">etat</th>

<th></th>






                                </tr>
                                </thead>
                                <tbody >
                                <tbody id="myTable">
                                {% for i in tablereclamation %}
                                    <tr>

                                    <tr>
                                        {#  <td class="text-center"><a type="button" href="{{ path('supprimertransport', {'id': transport.id}) }}" class="btn btn-rounded btn-outline-danger">Supprimer</a></td>

                                    <td class="text-center"><a type="button" href="{{ path('modifiertransport', {'id': transport.id}) }}" class="btn btn-rounded btn-outline-warning">Modifier</a></td>#}

                                    <td>{{(i.date).format('d/m/Y')}}</td>
                                    <td> {{ i.type  }} </td>
                                    <td> {{ i.etat  }} </td>



                                        <td class="text-center"><a type="button" href="{{ path('supprimerreclamation', {'id': i.id}) }}" class="btn btn-rounded btn-outline-danger">Supprimer</a></td>


                                    </tr>


                                {% endfor %}
                                </tbody>
                                </tbody id="search">
                            </table>

                            <div class="navigation">

                            </div>
                        </div>
                    </div>
                </div>
                <!-- /# card -->
            </div>
        </div>
    </div>

{% endblock %}

